Zentinull is a device entity resolution pipeline (6 IT sources -> SQLite -> Splink -> FastAPI/DuckDB). Key principles: separate ingest from resolution, only device records go through Splink (metrics load directly via host resolution), use env-driven Splink thresholds, and enforce strict API contracts. Common pitfalls: DuckDB BIGINT inference (force all_varcharm), Pydantic v2 silently stripping response fields, ingestor field-name mismatches causing empty Splink columns, and comma-separated MAC normalization. Use parallel read-only scouts for audits and apply 12 operational protocols.

CURRENT STATE (2026-07-15): Phase 0-7 manifest refactor committed (git 880447f). 621 tests pass. On top of that commit: uncommitted Valentine schema-matching enrichment layer (valentine.py with COMA library, optional dep with MANUAL_REGISTRY fallback), paged_json_detail fetch strategy (list + per-record detail enrichment), pipeline now 6 stages (ingest→export→splink→load→valentine→attach), DuckDB v_extra + v_device_enriched auto-generated views, /device/{query}/trace mesh graph endpoint, datetime→str conversion in db.py (fixes bench-api), background scheduler in server.py, name_fallback conditional blocking field in manifest, atomic CSV export via .tmp+swap. New profile fields must be propagated to SourceRecord model, DEVICES_SQL (or contract test exclusion list), and incremental load test fixtures.